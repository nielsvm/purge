services:
  cache_tags.purge_cachetags_queuer:
    class: Drupal\purge_cachetags_queuer\CacheTagsInvalidationQueuer
    arguments: ['@purge.queue', '@purge.purgeables']
    tags:
      - { name: cache_tags_invalidator}
