services:
  purge_core_invalidation.listener:
    class: Drupal\purge_core_invalidation\CacheTagDeletionListener
    arguments: ['@purge.queue', '@purge.purgeables']
    tags:
      - { name: cache.bin }
