name: Purge runtime processor
type: module
description: 'Processes purge queue items during the same request, only for high-performance purgers!'
package: "Purge - processors"
version: VERSION
core: 8.x
dependencies:
  - purge
