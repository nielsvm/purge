In your VCL file:

sub vcl_recv {


  if (req.request == "BAN") {
    if (!client.ip ~ internal) {
      error 405 "Method not allowed";
    }
    ban("obj.http.X-Drupal-Cache-Tags ~ " + req.http.X-Drupal-Cache-Tags-Banned);
    error 200 "Banned" ;
  }
}
