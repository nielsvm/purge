<?PHP

/**
 * @file
 * Installation file of the purge_ui module.
 */

/**
 * Implementation of hook_requirements().
 */
function purge_ui_requirements($phase) {
  if ($phase !== 'runtime') {
    return [];
  }
  return \Drupal::service('purge.diagnostics')->getHookRequirementsArray();
}
