name: Purge API
type: module
description: 'Purge provides a generic (external) cache invalidation API. This module provides the means to purge and queue purge instructions.'
package: Purge
version: VERSION
core: 8.x
dependencies:
  - system
