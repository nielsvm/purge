services:
  purge_queuer_test.queuera:
    class: Drupal\purge_queuer_test\QueuerA
    arguments: ['@config.factory', '@purge.queue', '@purge.invalidation.factory']
    tags:
      - { name: purge_queuer }
  purge_queuer_test.queuerb:
    class: Drupal\purge_queuer_test\QueuerB
    arguments: ['@config.factory', '@purge.queue', '@purge.invalidation.factory']
    tags:
      - { name: purge_queuer }
  purge_queuer_test.queuerc:
    class: Drupal\purge_queuer_test\QueuerC
    arguments: ['@config.factory', '@purge.queue', '@purge.invalidation.factory']
    tags:
      - { name: purge_queuer }
